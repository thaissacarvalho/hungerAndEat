/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'spoonacular.com',
            'smashboard.org',
        ],
    }
};

export default nextConfig;
